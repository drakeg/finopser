# Service-principal scope boundary

Service-bound credentials use the same approved read-only scopes as human-owned API credentials. Service principals do not create a second authorization vocabulary and do not grant write or remediation access.
